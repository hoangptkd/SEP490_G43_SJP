spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/smart_recruitment_dev
    username: postgres
    password: postgres

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE